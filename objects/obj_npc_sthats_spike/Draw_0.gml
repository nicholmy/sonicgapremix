/// @description Insert description here
// You can write your code in this editor
if (isScared) {
	draw_sprite(spr_badnik_spike2, image_index, x, y);
} else {
	draw_sprite(spr_badnik_spike, image_index, x, y);
}