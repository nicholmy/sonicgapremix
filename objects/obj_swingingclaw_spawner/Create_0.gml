/// @description Initialize the base, chain, and claw

// Inherit the parent event
event_inherited();

/*base = instance_create(baseX, baseY, obj_swingingclaw_base)
base.depth = 1
claw = instance_create(baseX, baseY - chainLength, obj_swingingclaw_claw)

claw.baseX = x
claw.baseY = y
claw.chainLength = chainLength
claw.depth = 1*/

endResource.baseX = x
endResource.baseY = y
endResource.chainLength = chainLength