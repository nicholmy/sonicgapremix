/// @description Set up rings and Delay the timer
// Make sure there's aspecial ring spawner somewhere in the room
specialRing = instance_create(obj_ring_special_spawner.x, obj_ring_special_spawner.y, obj_ring_special);
currentTime = timeDuration;